 #include<stdio.h>
 int main()
     {
     	int n,i=1;
     	printf("enter n value");
     	scanf("%d",&n);
     	x:
     	printf("%d*%d=%d\n",n,i,n*i);
     	i++;
     	if(i<=20)
     	goto x;
     	return 0;
	 }
